#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 1 ]]; then
  echo "Usage: $0 /restore/target" >&2
  exit 2
fi

job_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$job_dir/restore.sh" --snapshot latest "$1"
